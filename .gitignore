# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Build output
.next/
out/
dist/
build/

# Environment — NEVER commit credentials
.env
.env.local
.env.development.local
.env.test
.env.test.local
.env.production.local

# Supabase local
supabase/.branches
supabase/.temp

# Vercel
.vercel

# Testing
coverage/
.nyc_output/

# OS / Editor
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/settings.json

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
dev-server*.log

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Sanity
.sanity/

.vercel
.env*
!.env*.example
.agent/reports/
# Ephemeral probe/audit/verify scratch under .agent/ (tracked: *.md, phase-state.json)
.agent/probe-*
.agent/audit-p*
.agent/verify-*
.agent/fix*-*.sql
.agent/lh-*.json
.agent/lighthouse-*.json
.agent/anon-execute-*.json
.agent/perf-baseline.csv
.agent/fn-bodies.json
.agent/cowork-*.patch
scratch/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Sentry Config File
.env.sentry-build-plugin

# Impeccable live helper runtime state (per-session, ephemeral)
.impeccable/live/server.json
.impeccable/live/sessions/
.claude/settings.local.json

# Claude Code parallel worktrees — ephemeral sibling sessions, never index
.claude/worktrees/

# Local screenshot scratch (Claude Code visual checks, never index)
.tmp-screenshots/
